


{% extends 'base.html.twig' %}


        {% block body %}
          
        <div ><h1 class="text-center">{{ article.title }}</h1>
        <p class="text-center">{{ article.status }}</p>
        
        </div>
        <hr>
        
        {% endblock %}